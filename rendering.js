Historically, search engine crawlers do not support JavaScript. For apps built entirely with React,
this means the crawler is seeing a blank page. That means your site won't show up in search engines at all.

Fixing this problem could be the single biggest reason to go for server-side rendering.